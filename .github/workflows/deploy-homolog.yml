name: 🧪 Deploy para Homolog

# Este workflow é acionado em dois momentos:
on:
  # 1. Quando há um push (envio de código) APENAS na branch 'develop'
  push:
    branches:
      - develop
  
  # 2. Permite acionamento manual (opcional, mas bom para demo)
  workflow_dispatch:

jobs:
  build_e_deploy_homolog:
    # Nome do Job
    name: Publicar em Homolog
    # Máquina onde roda
    runs-on: ubuntu-latest
    
    steps:
      - name: 🔎 Checkout do Codigo
        uses: actions/checkout@v4
        
      # Substitua este ECHO pelo seu comando de Build/Deploy real
      - name: 🚀 DEPLOY - Homologacao
        run: |
          echo "================================================="
          echo "  DEPLOY PARA HOMOLOGAÇÃO (Branch: develop)  "
          echo "================================================="
          echo "Projeto publicado com sucesso no ambiente de HOMOLOG!"
          echo "Versão do Commit: ${{ github.sha }}"
